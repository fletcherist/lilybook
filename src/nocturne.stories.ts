import { story, stories } from './App';
import { bassFisMinor, lick } from './nocturne';

export const storyBassFisMinor = story('bass f minor', bassFisMinor);
export const storyLick = story('jazz lick', lick);
export default stories('nocturne');
